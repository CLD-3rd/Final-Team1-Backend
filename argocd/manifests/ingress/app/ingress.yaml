# manifests/ingress/app/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: app
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  rules:
    - http:
        paths:
          - path: /api/content
            pathType: Prefix
            backend:
              service:
                name: mindscape-service
                port:
                  number: 8080
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: mindscape-auth
                port:
                  number: 8080
          - path: /api/test
            pathType: Prefix
            backend:
              service:
                name: mindscape-info
                port:
                  number: 8080
